import type { AssignmentStatus } from '../types'

export const STATUS_LABEL: Record<AssignmentStatus, string> = {
  'as-requested': 'Wie gewünscht',
  alternative: 'Alternative',
  unresolved: 'Nicht erfüllbar',
}
